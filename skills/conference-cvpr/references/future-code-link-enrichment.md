# Future Code Link Enrichment

Code and project URL enrichment is reserved for future versions.

Potential sources:

- official paper pages when CVF exposes links
- author project pages
- Papers With Code
- GitHub Search

Do not call these sources in v1. Keep `code_url` and `project_url` empty unless the value is already present in user-provided data.
